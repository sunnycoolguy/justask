import 'package:flutter/material.dart';
import '../CurrentPageEnum.dart';

class JoinClassroom extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text('Enter the classroom number!');
  }
}
